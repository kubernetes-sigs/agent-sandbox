apiVersion: v1
kind: Service
metadata:
  name: openclaw-gateway
spec:
  type: LoadBalancer
  selector:
    sandbox: openclaw-template-sandbox
  ports:
    - name: gateway
      port: 18789
      targetPort: 18789