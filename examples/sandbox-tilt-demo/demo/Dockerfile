# Minimal demo image for the Agent Sandbox Tilt demo.
# Echoes a message and stays alive so the Sandbox pod can be inspected while
# `tilt up` is running; it is torn down when you run `tilt down`.
FROM alpine:latest

CMD ["sh", "-c", "echo 'Hello from an Agent Sandbox!' && sleep 3600"]
