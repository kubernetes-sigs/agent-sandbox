agent-sandbox has been installed.

{{- if .Values.controller.watchNamespace }}

NOTE: Namespaced mode is enabled (watchNamespace={{ .Values.controller.watchNamespace }}).

{{- $controllerNamespace := include "agent-sandbox.namespace" . }}
{{- $leaderElectionNamespace := include "agent-sandbox.leaderElectionNamespace" . }}

NOTE: ClusterRoles and ClusterRoleBindings are not rendered in namespaced mode.
The chart does not generate workload or leader-election Roles and RoleBindings
for that mode. Advanced users must set up the appropriate Roles and RoleBindings
for the watched namespace(s) ({{ .Values.controller.watchNamespace }}) and bind
them to the agent-sandbox-controller ServiceAccount in {{ $controllerNamespace }}.
{{- if .Values.controller.leaderElect }}

WARNING: Leader election is enabled (controller.leaderElect=true, the
default). The controller will attempt to acquire a Lease in
{{ $leaderElectionNamespace }} on startup, but this chart does not render the
required Role/RoleBinding granting `coordination.k8s.io/leases` permissions
there. Without it, the controller pod will run but fail to acquire leadership
(403 Forbidden) and will never reconcile. You must create a Role/RoleBinding
granting get/list/watch/create/update/patch on `leases` in
{{ $leaderElectionNamespace }}, bound to the agent-sandbox-controller
ServiceAccount in {{ $controllerNamespace }} — or set
controller.leaderElect=false if running a single replica without leader
election.
{{- end }}

{{- end }}
