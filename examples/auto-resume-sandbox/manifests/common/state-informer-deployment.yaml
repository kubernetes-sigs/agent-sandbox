apiVersion: apps/v1
kind: Deployment
metadata:
  name: sandbox-state-informer
  namespace: default
  labels:
    app: sandbox-state-informer
spec:
  replicas: 2
  selector:
    matchLabels:
      app: sandbox-state-informer
  template:
    metadata:
      labels:
        app: sandbox-state-informer
    spec:
      serviceAccountName: sandbox-state-informer-sa
      containers:
      - name: sandbox-state-informer
        image: sandbox-state-informer:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 50051
          name: grpc-ext-proc
        env:
        - name: PORT
          value: "50051"
        - name: MANAGER_ENDPOINT
          value: "http://sandbox-suspension-manager-svc:8090/v1/sandboxes/resume"
---
apiVersion: v1
kind: Service
metadata:
  name: sandbox-state-informer-svc
  namespace: default
spec:
  selector:
    app: sandbox-state-informer
  ports:
  - name: grpc-ext-proc
    port: 50051
    targetPort: 50051
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: sandbox-state-informer-sa
  namespace: default
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: sandbox-state-informer-cluster-role
rules:
- apiGroups: [""]
  resources: ["pods"]
  verbs: ["get", "list", "watch"]
- apiGroups: ["agents.x-k8s.io"]
  resources: ["sandboxes"]
  verbs: ["get", "list", "watch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: sandbox-state-informer-cluster-binding
subjects:
- kind: ServiceAccount
  name: sandbox-state-informer-sa
  namespace: default
- kind: ServiceAccount
  name: sandbox-state-informer-sa
  namespace: tenant-a
- kind: ServiceAccount
  name: sandbox-state-informer-sa
  namespace: tenant-b
roleRef:
  kind: ClusterRole
  name: sandbox-state-informer-cluster-role
  apiGroup: rbac.authorization.k8s.io

