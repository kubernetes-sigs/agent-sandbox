# 1. sandbox-router Deployment in Tenant-A
apiVersion: apps/v1
kind: Deployment
metadata:
  name: sandbox-router
  namespace: tenant-a
  labels:
    app: sandbox-router
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sandbox-router
  template:
    metadata:
      labels:
        app: sandbox-router
    spec:
      containers:
      - name: sandbox-router
        image: sandbox-router:latest
        imagePullPolicy: IfNotPresent
        args:
        - --http-bind-address=:8080
        - --health-probe-bind-address=:8081
        - --cluster-domain=cluster.local
        - --proxy-timeout=180s
        - --cache-enabled=false
        ports:
        - containerPort: 8080
          name: http
---
# 2. Service for sandbox-router in Tenant-A
apiVersion: v1
kind: Service
metadata:
  name: sandbox-router-svc
  namespace: tenant-a
spec:
  selector:
    app: sandbox-router
  ports:
  - port: 8080
    targetPort: 8080
---
# 3. Static Wildcard HTTPRoute referencing sandbox-router Service
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: tenant-a-sandboxes-route
  namespace: tenant-a
spec:
  parentRefs:
  - name: sandbox-edge-gateway
    namespace: envoy-gateway-system
  hostnames:
  - "*.tenant-a.sandbox.local"
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /
    timeouts:
      request: 180s
    backendRefs:
    - name: sandbox-router-svc
      port: 8080
---
# 4. EnvoyExtensionPolicy attaching sandbox-state-informer ext_proc callout engine in tenant-a
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: EnvoyExtensionPolicy
metadata:
  name: tenant-a-auto-resume-policy
  namespace: tenant-a
spec:
  targetRefs:
  - group: gateway.networking.k8s.io
    kind: HTTPRoute
    name: tenant-a-sandboxes-route
  extProc:
  - backendRefs:
    - name: sandbox-state-informer-svc
      port: 50051
    messageTimeout: 180s
    failOpen: false
    processingMode:
      request: {}
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: sandbox-state-informer-sa
  namespace: tenant-a
---
# 5. Tenant-A sandbox-state-informer Callout Deployment (Pure gRPC Engine)
apiVersion: apps/v1
kind: Deployment
metadata:
  name: sandbox-state-informer
  namespace: tenant-a
  labels:
    app: sandbox-state-informer
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sandbox-state-informer
  template:
    metadata:
      labels:
        app: sandbox-state-informer
    spec:
      serviceAccountName: sandbox-state-informer-sa
      containers:
      - name: sandbox-state-informer
        image: sandbox-state-informer:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 50051
          name: grpc-ext-proc
        env:
        - name: PORT
          value: "50051"
        - name: MANAGER_ENDPOINT
          value: "http://sandbox-suspension-manager-svc.default.svc.cluster.local:8090/v1/sandboxes/resume"
---
# 6. Tenant-A sandbox-state-informer gRPC Callout Service
apiVersion: v1
kind: Service
metadata:
  name: sandbox-state-informer-svc
  namespace: tenant-a
spec:
  selector:
    app: sandbox-state-informer
  ports:
  - name: grpc-ext-proc
    port: 50051
    targetPort: 50051
---
# 7. BackendTrafficPolicy setting 180s HTTP request timeout for tenant-a cold-starts
apiVersion: gateway.envoyproxy.io/v1alpha1
kind: BackendTrafficPolicy
metadata:
  name: tenant-a-timeout-policy
  namespace: tenant-a
spec:
  targetRefs:
  - group: gateway.networking.k8s.io
    kind: HTTPRoute
    name: tenant-a-sandboxes-route
  timeout:
    http:
      requestTimeout: 180s



